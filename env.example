# =============================================================================
# Arquivo de Configuração de Ambiente - Exemplo
# =============================================================================
# 
# INSTRUÇÕES:
# 1. Copie este arquivo para .env (sem o .example)
# 2. Configure suas variáveis de ambiente abaixo
# 3. IMPORTANTE: Nunca commite o arquivo .env com suas chaves reais!
#
# =============================================================================

# -----------------------------------------------------------------------------
# Configurações da Aplicação
# -----------------------------------------------------------------------------

# Modo de debug (True/False)
# Quando True, mostra mais informações de log e recarrega automaticamente
DEBUG=False

# Porta do servidor (padrão: 8050)
# Pode ser alterada via variável de ambiente PORT
PORT=8050

# -----------------------------------------------------------------------------
# Configurações do Chatbot OpenAI
# -----------------------------------------------------------------------------

# Chave da API OpenAI (OBRIGATÓRIA para usar OpenAI)
# Obtenha sua chave em: https://platform.openai.com/api-keys
# Se não configurar, o sistema funcionará em modo fallback (respostas básicas)
OPENAI_API_KEY=sua_chave_api_openai_aqui

# Modelo do OpenAI a ser usado
# Opções recomendadas:
#   - gpt-4o-mini: Mais barato e rápido, ideal para produção
#   - gpt-3.5-turbo: Mais barato, bom para testes
#   - gpt-4: Mais caro, melhor qualidade
MODEL_NAME=gpt-4o-mini

# Número máximo de tokens na resposta (padrão: 500)
# Aumente se precisar de respostas mais longas
MAX_TOKENS=500

# Temperatura (0.0 a 1.0)
# 0.0 = respostas determinísticas e consistentes
# 1.0 = respostas mais criativas e variadas
# Recomendado: 0.7 para balancear consistência e criatividade
TEMPERATURE=0.7

# -----------------------------------------------------------------------------
# Configurações de Banco de Dados (Futuro)
# -----------------------------------------------------------------------------
# Caso precise conectar a um banco de dados no futuro:
# DATABASE_URL=postgresql://usuario:senha@host:porta/database
